import { ProductCameraMpxPipe } from './product-camera-mpx.pipe';

describe('ProductCameraMpxPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductCameraMpxPipe();
    expect(pipe).toBeTruthy();
  });
});
