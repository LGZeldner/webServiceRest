import { PhoneCameraMpxPipe } from './phone-camera-mpx.pipe';

describe('PhoneCameraMpxPipe', () => {
  it('create an instance', () => {
    const pipe = new PhoneCameraMpxPipe();
    expect(pipe).toBeTruthy();
  });
});
